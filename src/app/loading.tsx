export default function Loading() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "var(--content-secondary)",
        fontSize: 14,
        lineHeight: "20px",
      }}
    >
      Gathering the lore…
    </div>
  );
}
